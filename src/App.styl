@import './Global'

html {
  height: 100%
  box-sizing: border-box
}

*, *:before, *:after {
  box-sizing: inherit
}

body, #root {
  height: 100%
}

.App {
  display: flex
  flex-flow: row nowrap
  height: 100%
}
